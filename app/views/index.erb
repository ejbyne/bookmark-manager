<nav>
  Filter by tag:
  <br><br>
  <ul>
    <%@tags.each do |tag| %>
      <%= partial :tag, :locals => {:tag => tag} %>
    <% end %><br>
  <li><a href="/">reset</a></li>
  </ul>
</nav>

<section>
  <ul>
    <% @links.each do |link| %>
      <%= partial :link, :locals => {:link => link} %>
    <% end %>
  </ul>
</section>
