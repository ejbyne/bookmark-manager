<li class="link">
  <a class="title" href="<%= link.url %>"><%= link.title %></a>
  <div class="description">
    <%= link.description %>
  </div>
  <span class="source">
    added <%= link.created_at_string %> by
    <a href="#"><%= User.get(link.user_id).email %></a>
  </span>
</li>
